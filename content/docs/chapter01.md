---
title: "챕터 1: 설정과 기초"
weight: 1
bookToc: true
---

# 챕터 1: 설정과 기초

이 챕터에서는 백테스팅의 기본 개념을 이해하고, 필요한 도구들을 설치하여 개발 환경을 구축합니다. 또한 OHLCV 데이터의 구조를 파악하고, 첫 번째 간단한 백테스트를 실행해보겠습니다.

## 1.1 백테스팅이란 무엇이고 왜 중요한가

### 백테스팅의 정의

백테스팅(Backtesting)은 과거의 시장 데이터를 사용하여 투자 전략의 성과를 시뮬레이션하는 과정입니다. 이는 실제 자금을 투입하기 전에 전략의 유효성을 검증할 수 있는 핵심적인 도구입니다.

### 백테스팅의 수학적 기초

백테스팅에서 가장 기본적인 개념은 **수익률(Return)**입니다. 시점 t에서의 수익률은 다음과 같이 계산됩니다:

$$R_t = \frac{P_t - P_{t-1}}{P_{t-1}} = \frac{P_t}{P_{t-1}} - 1$$

여기서:
- $R_t$: 시점 t에서의 수익률
- $P_t$: 시점 t에서의 가격
- $P_{t-1}$: 시점 t-1에서의 가격

**누적 수익률(Cumulative Return)**은 다음과 같이 계산됩니다:

$$R_{cumulative} = \prod_{t=1}^{n}(1 + R_t) - 1$$

**연평균 수익률(Annualized Return)**은:

$$R_{annual} = (1 + R_{cumulative})^{\frac{252}{n}} - 1$$

여기서 252는 일반적인 연간 거래일 수입니다.

### 백테스팅이 중요한 이유

1. **리스크 관리**: 실제 투자 전에 잠재적 손실을 파악
2. **전략 검증**: 투자 아이디어의 실효성 확인
3. **매개변수 최적화**: 전략의 최적 설정값 탐색
4. **심리적 준비**: 예상되는 변동성과 손실에 대한 사전 경험

### 백테스팅의 한계

- **과최적화(Overfitting)**: 과거 데이터에만 맞춘 전략
- **생존자 편향(Survivorship Bias)**: 상장폐지된 종목 제외
- **전진 편향(Look-ahead Bias)**: 미래 정보의 부적절한 사용
- **거래비용 미반영**: 수수료, 슬리피지 등의 현실적 비용

## 1.2 개발 환경 설정

### uv를 사용한 Python 환경 구축

이 책에서는 최신 Python 패키지 관리자인 `uv`를 사용합니다. `uv`는 빠르고 효율적인 패키지 관리를 제공합니다.

#### uv 설치 (macOS)

```bash
# Homebrew를 사용한 설치
brew install uv

# 또는 curl을 사용한 설치
curl -LsSf https://astral.sh/uv/install.sh | sh
```

#### 프로젝트 초기화

```bash
# 프로젝트 디렉토리로 이동
cd codes

# 가상환경 생성 및 활성화 (이미 설정되어 있음)
uv sync
```

### 필요한 패키지들

우리가 사용할 주요 패키지들:

- **yfinance**: Yahoo Finance에서 주식 데이터 다운로드
- **pandas**: 데이터 조작 및 분석
- **matplotlib**: 데이터 시각화
- **backtrader**: 백테스팅 프레임워크
- **numpy**: 수치 계산
- **seaborn**: 고급 시각화

## 1.3 OHLCV 데이터 이해

### OHLCV 데이터 구조

OHLCV는 금융 시장 데이터의 표준 형식입니다:

- **Open**: 시가 (해당 기간의 첫 거래 가격)
- **High**: 고가 (해당 기간의 최고 거래 가격)
- **Low**: 저가 (해당 기간의 최저 거래 가격)
- **Close**: 종가 (해당 기간의 마지막 거래 가격)
- **Volume**: 거래량 (해당 기간의 총 거래 주식 수)

### 캔들스틱 차트의 수학적 해석

각 캔들스틱은 다음 정보를 제공합니다:

- **몸통(Body)**: |Close - Open|
- **위꼬리(Upper Shadow)**: High - max(Open, Close)
- **아래꼬리(Lower Shadow)**: min(Open, Close) - Low

**변동성(Volatility)**은 True Range로 측정할 수 있습니다:

$$TR = \max(High - Low, |High - Close_{prev}|, |Low - Close_{prev}|)$$

## 1.4 실습: yfinance로 데이터 가져오기

이제 실제 코드를 작성해보겠습니다. 먼저 NVIDIA 주식 데이터를 다운로드하는 예제를 만들어보겠습니다.

### 코드 실행하기

먼저 codes 디렉토리로 이동하여 첫 번째 예제를 실행해보겠습니다:

```bash
cd codes
uv run chapter01/01_basic_data_download.py
```

이 스크립트는 다음과 같은 작업을 수행합니다:

1. **NVIDIA 주식 데이터 다운로드**: yfinance를 사용하여 최근 1년간의 OHLCV 데이터를 가져옵니다.
2. **기본 통계 분석**: 수익률, 변동성, 캔들스틱 패턴 등을 계산합니다.
3. **시각화**: 가격 차트, 거래량, 수익률 분포 등을 그래프로 생성합니다.
4. **데이터 저장**: CSV 파일로 데이터를 저장하여 다음 예제에서 사용할 수 있도록 합니다.

### 실행 결과 분석

스크립트를 실행하면 다음과 같은 출력을 볼 수 있습니다:

```
=== NVIDIA 주식 데이터 다운로드 ===
회사명: NVIDIA Corporation
섹터: Technology
산업: Semiconductors
시가총액: $4,460,857,262,080
통화: USD

최근 1년 데이터 다운로드 중...
데이터 기간: 2024-10-18 ~ 2025-10-17
총 데이터 포인트: 250개

=== OHLCV 데이터 분석 ===
평균 일일 수익률: 0.0016 (0.16%)
일일 수익률 표준편차: 0.0312 (3.12%)
최대 일일 수익률: 0.1872 (18.72%)
최소 일일 수익률: -0.1697 (-16.97%)

평균 True Range: $5.39
평균 몸통 크기: $2.34
상승 캔들 비율: 54.80%
```

![NVIDIA 주가 및 거래량 차트](/images/chapter01/nvda_price_volume.png)

위 차트에서 볼 수 있듯이:
- **상단 차트**: NVIDIA의 주가 추이를 보여줍니다. 2024년 말부터 2025년 초까지 상당한 상승세를 보였습니다.
- **하단 차트**: 거래량을 나타냅니다. 주가 변동이 클 때 거래량도 함께 증가하는 패턴을 확인할 수 있습니다.

## 1.5 matplotlib을 사용한 기본 플롯팅

다음으로 matplotlib의 다양한 기능을 살펴보겠습니다:

```bash
uv run chapter01/02_matplotlib_basics.py
```

이 스크립트는 다음과 같은 차트들을 생성합니다:

### 라인 플롯의 기본 요소

```python
# 기본 라인 플롯
plt.plot(data.index, data['Close'], color='blue', linewidth=2)
plt.title('종가 추이')
plt.ylabel('가격 ($)')
plt.grid(True, alpha=0.3)
```

### 이동평균선 추가

```python
# 20일, 50일 이동평균 계산
data['MA_20'] = data['Close'].rolling(window=20).mean()
data['MA_50'] = data['Close'].rolling(window=50).mean()

# 차트에 표시
plt.plot(data.index, data['Close'], label='종가', color='black')
plt.plot(data.index, data['MA_20'], label='20일 이동평균', color='blue')
plt.plot(data.index, data['MA_50'], label='50일 이동평균', color='red')
```

### 캔들스틱 차트 구현

```python
for i, (idx, row) in enumerate(data.iterrows()):
    color = 'green' if row['Close'] >= row['Open'] else 'red'
    
    # 몸통 그리기
    body_height = abs(row['Close'] - row['Open'])
    body_bottom = min(row['Open'], row['Close'])
    ax.bar(i, body_height, bottom=body_bottom, width=0.6, 
           color=color, alpha=0.8)
    
    # 위아래 꼬리 그리기
    ax.plot([i, i], [row['Low'], row['High']], color='black', linewidth=1)
```

## 1.6 backtrader로 첫 번째 백테스트

이제 실제 백테스팅을 수행해보겠습니다:

```bash
uv run chapter01/03_first_backtest.py
```

### 매수 후 보유 전략 (Buy and Hold)

가장 간단한 투자 전략인 매수 후 보유 전략을 구현해보겠습니다:

```python
class BuyAndHoldStrategy(bt.Strategy):
    def next(self):
        if not self.position:
            if len(self.data) == 1:  # 첫 거래일에만 매수
                self.buy()
```

**실행 결과:**
```
=== 매수 후 보유 전략 백테스트 ===
초기 포트폴리오 가치: $10000.00
최종 포트폴리오 가치: $10044.99
총 수익률: 0.45%
벤치마크 수익률: 32.81%
```

### 단순 이동평균 전략

20일 이동평균을 기준으로 한 전략을 구현해보겠습니다:

```python
class SimpleMovingAverageStrategy(bt.Strategy):
    def __init__(self):
        self.sma = bt.indicators.SimpleMovingAverage(period=20)
        
    def next(self):
        if not self.position:
            if self.dataclose[0] > self.sma[0]:  # 매수 신호
                self.buy()
        else:
            if self.dataclose[0] < self.sma[0]:  # 매도 신호
                self.sell()
```

**실행 결과:**
```
=== 이동평균 전략 백테스트 ===
초기 포트폴리오 가치: $10000.00
최종 포트폴리오 가치: $9965.01
총 수익률: -0.35%
```

### 전략 비교 분석

두 전략의 성과를 비교해보면:

| 전략 | 최종 가치 | 수익률 |
|------|-----------|--------|
| 매수 후 보유 | $10,044.99 | 0.45% |
| 이동평균 전략 | $9,965.01 | -0.35% |
| 벤치마크 (NVDA) | - | 32.81% |

**주요 관찰사항:**

1. **벤치마크와의 차이**: 두 전략 모두 단순히 NVIDIA 주식을 보유하는 것보다 훨씬 낮은 수익률을 보였습니다. 이는 다음과 같은 이유 때문입니다:
   - **거래 비용**: 0.1%의 수수료가 수익률을 감소시켰습니다.
   - **현금 보유**: 전략에서 일부 기간 동안 현금을 보유하여 주식 상승의 혜택을 완전히 받지 못했습니다.

2. **이동평균 전략의 한계**: 횡보장에서는 잦은 매매로 인한 손실이 발생할 수 있습니다.

## 1.7 백테스팅의 핵심 개념

### 성과 측정 지표

백테스팅에서 중요한 성과 지표들:

1. **총 수익률 (Total Return)**:
   $$\text{Total Return} = \frac{\text{Final Value} - \text{Initial Value}}{\text{Initial Value}} \times 100$$

2. **연평균 수익률 (Annualized Return)**:
   $$\text{Annualized Return} = \left(\frac{\text{Final Value}}{\text{Initial Value}}\right)^{\frac{252}{n}} - 1$$

3. **최대 손실 (Maximum Drawdown)**:
   $$\text{MDD} = \max\left(\frac{\text{Peak} - \text{Trough}}{\text{Peak}}\right)$$

4. **샤프 비율 (Sharpe Ratio)**:
   $$\text{Sharpe Ratio} = \frac{\text{Return} - \text{Risk-free Rate}}{\text{Standard Deviation}}$$

### 백테스팅 시 주의사항

1. **과최적화 (Overfitting)**: 과거 데이터에만 맞춘 전략은 미래에 작동하지 않을 수 있습니다.

2. **생존자 편향 (Survivorship Bias)**: 상장폐지된 종목을 제외하면 실제보다 좋은 결과가 나올 수 있습니다.

3. **전진 편향 (Look-ahead Bias)**: 미래 정보를 사용하면 안 됩니다.

4. **거래 비용**: 수수료, 슬리피지, 세금 등을 반드시 고려해야 합니다.

## 1.8 다음 단계

챕터 1에서 학습한 내용:

✅ **백테스팅의 기본 개념과 중요성**  
✅ **Python 환경 설정 (uv, yfinance, matplotlib, backtrader)**  
✅ **OHLCV 데이터 구조와 분석**  
✅ **matplotlib을 사용한 기본 시각화**  
✅ **backtrader로 첫 번째 백테스트 실행**  
✅ **매수 후 보유 vs 이동평균 전략 비교**

### 실습 과제

1. **데이터 탐색**: 다른 종목(예: AAPL, MSFT)의 데이터를 다운로드하고 분석해보세요.

2. **매개변수 변경**: 이동평균 기간을 10일, 30일, 50일로 변경하여 성과를 비교해보세요.

3. **시각화 개선**: 차트에 거래 신호(매수/매도 포인트)를 표시해보세요.

### 다음 챕터 미리보기

**챕터 2: 데이터 준비 - NVIDIA 주식 데이터 다운로드**에서는:
- 다양한 기간의 데이터 수집 (1년, 5년, 10년)
- 데이터 정제 및 전처리 기법
- 데이터 품질 검증 방법
- 효율적인 데이터 관리 시스템 구축

---

**💡 팁**: 백테스팅은 과거 데이터를 기반으로 한 시뮬레이션입니다. 실제 투자에서는 시장 상황, 심리적 요인, 예상치 못한 사건 등이 결과에 영향을 미칠 수 있습니다. 백테스팅 결과를 맹신하지 말고, 다양한 시나리오를 고려하여 전략을 개발하세요.